{
  "name": "benchmark-async-await",
  "version": "1.0.0",
  "description": "Benchmark async/await with transpiling",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
